class HomeScreen extends StatelessWidget {
  // UI showing recent recipes and navigation to other screens.
}

class RecipeSearchScreen extends StatelessWidget {
  // UI to input ingredients manually or scan the fridge, and display matching recipes.
}

class SavedRecipesScreen extends StatelessWidget {
  // UI to show saved recipes.
}

class ProfileScreen extends StatelessWidget {
  // UI for user profile with the ability to log out.
}